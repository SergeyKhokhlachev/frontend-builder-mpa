mixin breadcrumbs(data = [], options = {})
	nav.breadcrumbs&attributes(attributes)
		.breadcrumbs__scroll
			ul.breadcrumbs__list
				each href, name in data
					li.breadcrumbs__item
						if href
							a(href=href aria-label=name)= name
							+icon('chevron-right')
						else
							span= name
