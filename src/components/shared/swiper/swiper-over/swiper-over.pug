mixin swiper-over(props = {})
	-
		props = {
			id: props.id || null,
			class: props.class || null,
		}

	.swiper-over&attributes(attributes)
		.swiper(id=props.id, class=props.class)
			.swiper-wrapper
				block

		.swiper-pagination.js-swiper-pagination
